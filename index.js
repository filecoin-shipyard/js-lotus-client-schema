const {
  common,
  fullNode,
  storageMiner,
  gatewayApi,
  walletApi,
  workerApi
} = require('./mainnet')

const mainnet = {
  common,
  fullNode,
  storageMiner,
  gatewayApi,
  walletApi,
  workerApi
}

module.exports = { mainnet }
